    <!-- <Button variant="contained"  className='start-button' onClick={()=>setStartQuiz(!startQuiz) }> Start </Button> -->
<!-- const [startQuiz,setStartQuiz] = useState(false);
  
  // const startHandler = ()=>{
  //   setStartQuiz(true)
  //   console.log("clicked!")
  // } -->
  <!-- return (
    <div className="App">
    <>
    <header className="App-header">
    <h1 className='title'>Quiz App</h1>
      </header>
      <div>
      <form className='cover'>
      <div className="question-text">
                {query[currentQuestion].question}
              </div>
              <div className="answer-section">
              {query[currentQuestion].answerOption.map((answerOption) => (
                <button
                  onClick={() =>
                    handleAnswerButtonClick(answerOption.isCorrect)
                  }
                >
                  {answerOption.answerText}
                </button>
              ))}
              </div>
    </form>
    </div>
    </>
    </div>
  ) -->
<!-- 
  {showScore ? (
    <div className="score-section">
      You scored {score} out of {query.length}
    </div>
  ) : ( -->
............................................................................................................................

  <!-- onClick={() =>
    handleAnswerButtonClick(answerOption.isCorrect)
  }

  <div className="score-section">
    You scored {score} out of {query.length}
  </div>
  <div className="question-section">
            <div className="question-count">
              <span>Question {currentQuestion+1}</span>
              
            </div>
            <br></br>
           
            <div className="question-text">
              {query[0].question}
            </div>
           
          </div>
           
          <div className="answer-section">
            
            {query[0].answerOption.map((answerOption) => (
              
              <ul>                
                <li className='list'>
                {answerOption.answer}
              </li>
             
              </ul>
             ))}
          </div> -->

          <!-- onClick={()=>{ const confirmBox = window.confirm(
            "Do you really want to delete this Crumb?")}} -->

            <!-- style={{
              backgroundColor: true ? 'green' : 'red'
            }} -->
            ...................................................................................................
            <!-- <div class=" style= {{backgroundColor:  prime.iscorrect== false?'red':'white',}}"></div> -->